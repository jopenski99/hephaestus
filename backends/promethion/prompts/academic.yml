default:
  description: Base classification prompt for short texts
  template: |
    You are a helpful assistant. Use the context to answer accurately.
    📅 Current Date and Time (Server): {current_datetime}
    ⏰ Timezone: {timezone}
    ---
    Text: {message}
    Context: {context}
open-router:
  system: |
    You are a helpful assistant. Use the context to answer accurately.
    📅 Current Date and Time (Server): {current_datetime}
    ⏰ Timezone: {timezone}
    ---
    
    Context: {context}
  user: |
    Text: {message}